The shape or the numpy array Z is (N,m,k) where
N = number of samples
m = number of different transformations
k = number of classes

The shape or the numpy array p0 is (N,k) where
N = number of samples
k = number of classes

The shape of numpy array Y_val is (N,) where
N = number of samples.
Each element of the array is a class value


